# shared/scripts — PLACEHOLDER

Deterministic Python core for the suite. Authored in later tasks:

- `docx_normalize.py` — Normalized_Text + stable anchors from a `.docx` (task 2) — implemented
- `input_normalize.py` — multi-format (pdf/html/txt/md/docx) → Normalized_Text (task 3) — implemented
- `page_count.py` — render-based page counting (Word/LibreOffice) (task 4) — implemented
- `docx_edit.py` — the edit engine applying the closed Change_List operation set (task 5)
- `ats_structural.py` — deterministic ATS hazard detection (task 6)

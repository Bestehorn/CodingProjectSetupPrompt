{
  "_comment": "Copy to config/aws_accounts.json (gitignored) and replace placeholders. The 'profile' field must match a named profile in your AWS credentials file. Never set AWS_PROFILE or AWS_REGION as shell environment variables — boto3 sessions must take profile_name and region_name as explicit arguments resolved through src/aws_config.py.",
  "accounts": {
    "dev": {
      "account_id": "REPLACE_WITH_DEV_ACCOUNT_ID",
      "profile": "REPLACE_WITH_DEV_NAMED_PROFILE",
      "region": "us-east-1",
      "description": "Development environment"
    },
    "staging": {
      "account_id": "REPLACE_WITH_STAGING_ACCOUNT_ID",
      "profile": "REPLACE_WITH_STAGING_NAMED_PROFILE",
      "region": "us-east-1",
      "description": "Staging environment"
    },
    "prod": {
      "account_id": "REPLACE_WITH_PROD_ACCOUNT_ID",
      "profile": "REPLACE_WITH_PROD_NAMED_PROFILE",
      "region": "us-east-1",
      "description": "Production environment"
    }
  }
}
